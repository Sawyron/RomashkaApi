spring:
  application.name: romashka-api
  datasource:
    url: jdbc:postgresql://localhost:5432/romashka
    username: postgres
    password: qwerty
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
